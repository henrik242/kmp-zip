name: CI

permissions:
  contents: read

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  build-and-test:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            tasks: ":kmp-zip:jvmTest :kmp-zip:iosSimulatorArm64Test :kmp-zip:macosArm64Test :kmp-zip:jsNodeTest :kmp-zip:wasmJsNodeTest :samples:browser-demo:jsNodeTest :samples:browser-demo:wasmJsNodeTest :kmp-zip-okio:check :kmp-zip-kotlinx:check :kmp-zip-cli:jvmTest :kmp-zip-cli:macosArm64Test"
            cli-link-tasks: ":kmp-zip-cli:linkReleaseExecutableMacosArm64 :kmp-zip-cli:linkReleaseExecutableMacosX64"
            primary: true
          - os: ubuntu-latest
            tasks: ":kmp-zip:jvmTest :kmp-zip:linuxX64Test :kmp-zip-okio:linuxX64Test :kmp-zip-kotlinx:linuxX64Test :kmp-zip-cli:jvmTest :kmp-zip-cli:linuxX64Test"
            cli-link-tasks: ":kmp-zip-cli:linkReleaseExecutableLinuxX64 :kmp-zip-cli:linkReleaseExecutableLinuxArm64"
            primary: false
          - os: windows-latest
            tasks: ":kmp-zip:jvmTest :kmp-zip:mingwX64Test :kmp-zip-okio:mingwX64Test :kmp-zip-kotlinx:mingwX64Test :kmp-zip-cli:jvmTest :kmp-zip-cli:mingwX64Test"
            cli-link-tasks: ":kmp-zip-cli:linkReleaseExecutableMingwX64"
            primary: false
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: 21
      - uses: gradle/actions/setup-gradle@v6
        with:
          # v6 made the default cache provider proprietary.
          cache-provider: basic
      - uses: actions/cache@v6
        with:
          path: ~/.konan
          key: konan-${{ runner.os }}-${{ hashFiles('gradle/libs.versions.toml') }}
          restore-keys: konan-${{ runner.os }}-
      - name: Run tests
        shell: bash
        run: ./gradlew ${{ matrix.tasks }}
      - name: Build all targets and publish to Maven Local
        if: matrix.primary
        shell: bash
        run: ./gradlew build publishToMavenLocal
      - name: Check Java 8 artifacts
        if: matrix.primary
        shell: bash
        run: scripts/check-java8-artifacts.sh
      - name: Link release CLI binaries
        shell: bash
        run: ./gradlew ${{ matrix.cli-link-tasks }}
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: test-reports-${{ matrix.os }}
          path: "**/build/reports/tests/"
      - uses: actions/upload-artifact@v7
        if: matrix.os == 'macos-latest'
        with:
          name: kmpzip-macos-arm64
          path: kmp-zip-cli/build/bin/macosArm64/releaseExecutable/kmpzip-macos-arm64
          archive: false
          if-no-files-found: error
      - uses: actions/upload-artifact@v7
        if: matrix.os == 'macos-latest'
        with:
          name: kmpzip-macos-x64
          path: kmp-zip-cli/build/bin/macosX64/releaseExecutable/kmpzip-macos-x64
          archive: false
          if-no-files-found: error
      - uses: actions/upload-artifact@v7
        if: matrix.os == 'ubuntu-latest'
        with:
          name: kmpzip-linux-x64
          path: kmp-zip-cli/build/bin/linuxX64/releaseExecutable/kmpzip-linux-x64
          archive: false
          if-no-files-found: error
      - uses: actions/upload-artifact@v7
        if: matrix.os == 'ubuntu-latest'
        with:
          name: kmpzip-linux-arm64
          path: kmp-zip-cli/build/bin/linuxArm64/releaseExecutable/kmpzip-linux-arm64
          archive: false
          if-no-files-found: error
      - uses: actions/upload-artifact@v7
        if: matrix.os == 'windows-latest'
        with:
          name: kmpzip-windows-x64.exe
          path: kmp-zip-cli/build/bin/mingwX64/releaseExecutable/kmpzip-windows-x64.exe
          archive: false
          if-no-files-found: error
      - uses: actions/upload-artifact@v7
        if: matrix.primary
        with:
          name: maven-artifacts
          path: ~/.m2/repository/no/synth/

  # JDK 8 is installed first so JAVA_HOME stays on 21, and setup-gradle must run
  # after both: it is what registers the installed JDKs as Gradle toolchains.
  java8-runtime:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: 8
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: 21
      - uses: gradle/actions/setup-gradle@v6
        with:
          cache-provider: basic
      - uses: actions/cache@v6
        with:
          path: ~/.konan
          key: konan-metadata-${{ runner.os }}-${{ hashFiles('gradle/libs.versions.toml') }}
          restore-keys: konan-metadata-${{ runner.os }}-
      - name: Run JVM tests on JDK 8
        shell: bash
        run: >-
          ./gradlew -Ptest.jvm=8
          :kmp-zip:jvmTest :kmp-zip-okio:jvmTest :kmp-zip-kotlinx:jvmTest
      - name: Publish kmp-zip to Maven Local
        shell: bash
        run: >-
          ./gradlew :kmp-zip:publishJvmPublicationToMavenLocal
          :kmp-zip:publishKotlinMultiplatformPublicationToMavenLocal
      - name: Java 8 consumer smoke test
        shell: bash
        run: |
          version=$(./gradlew -q :kmp-zip:properties --property version | awk '/^version:/ {print $2}')
          ./gradlew -p scripts/java8-consumer run -PkmpzipVersion="$version"
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: test-reports-java8
          path: "**/build/reports/tests/"
